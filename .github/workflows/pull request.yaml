name: On Pull Request

on:
  pull_request:
    branches:
      - main

jobs:
  pr-check:
    runs-on: ubuntu-latest
    steps:
      - name: Run on Pull Request
        run: echo "Pull request opened against main!"
